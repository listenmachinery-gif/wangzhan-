# Fully Automatic Three-Roll Plate Rolling Machine Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Execute inline with test-driven development because the user requested uninterrupted direct implementation. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build and deploy a dedicated Fully Automatic Three-Roll Plate Rolling Machine CNC solution page using original parameters, unique real application photography, responsive specifications, and safe SEO.

**Architecture:** Keep the existing dynamic product route and product record, dispatch this one product ID to a dedicated server component, and place page content plus presentation metadata in one typed data module. Protect the route with a focused source contract and keep application assets in a product-specific public directory with provenance notes.

**Tech Stack:** Next.js 15 App Router, React 19 server components, TypeScript, Tailwind CSS, Next Image, Node contract verifiers.

## Global Constraints

- Keep the original product image and the four existing parameter values unchanged.
- Use `Customizable / Please confirm with our sales engineer` for unconfirmed fields.
- Use twelve real application photographs that do not duplicate the semi-automatic or electric two-roll page.
- Do not add fake machine-detail images, line-art application images, numeric capabilities, prices, QR codes, or dependencies.
- Keep parameter labels together on one line; center units below them; scroll tables locally on mobile.
- Keep all configuration-dependent automation and structure claims explicitly optional.

---

### Task 1: Define the dedicated page contract

**Files:**
- Create: `scripts/verify-fully-automatic-three-roll-plate-rolling-machine-page.mjs`
- Modify: `package.json`

- [ ] Write assertions for the focused script, route guard, SEO, one H1, ordered 20 sections, structured data, original specification indexes, unique real assets, table formatting, and prohibited claims.
- [ ] Run `npm run verify:fully-automatic-three-roll-rolling` and observe failure because the dedicated implementation does not exist.

### Task 2: Add page content and real application photography

**Files:**
- Create: `data/fully-automatic-three-roll-plate-rolling-machine-page.ts`
- Create: `public/products/fully-automatic-three-roll-plate-rolling-machine-applications/*.webp`
- Create: `public/products/fully-automatic-three-roll-plate-rolling-machine-applications/SOURCES.md`

- [ ] Add typed content for every section and preserve original specifications through source indexes `0..3`.
- [ ] Source twelve unique public-license real photos, convert them to optimized WebP without geometric alteration, and record creator, source, license, mapping, and disclaimer.
- [ ] Run the focused verifier and confirm asset/data assertions advance while route/component assertions remain red.

### Task 3: Implement the dedicated server page

**Files:**
- Create: `components/FullyAutomaticThreeRollPlateRollingMachineSolutionPage.tsx`

- [ ] Render safe `ProductModel`, `FAQPage`, and `BreadcrumbList` JSON-LD and the existing product image.
- [ ] Render all 20 sections with semantic markup, configuration-safe wording, real photographs, CSS technical diagrams, and native FAQ disclosures.
- [ ] Resolve original parameters from `product.specs`, add confirmation placeholders, keep heading labels unbroken, put units on centered second lines, and make tables locally scrollable.

### Task 4: Route the product and add SEO

**Files:**
- Modify: `app/products/[id]/page.tsx`
- Modify: `scripts/verify-all-product-solution-pages.mjs`

- [ ] Add the exact metadata title, description, keywords, canonical, Open Graph, Twitter, and image alt.
- [ ] Dispatch the exact product ID before the generic route.
- [ ] Add this product to the dedicated page count without changing the 53-product total.
- [ ] Run the focused contract and all solution-page contracts.

### Task 5: Verify and deploy

**Files:**
- Verify only.

- [ ] Run every `verify:*` script, `npm run lint`, and `npm run build` using bounded logs.
- [ ] Inspect desktop and `390 x 844` mobile layouts, confirm 20 sections, one H1, all twelve photos, no overlay/errors/overflow, table scrolling, and one opened FAQ.
- [ ] Commit the complete scoped change, push `main`, wait for Vercel production success, and verify the live page marker, title, content, images, and route status.
